{
  "_comment": [
    "EXAMPLE ONLY - contains no real secret and will not authenticate.",
    "Do NOT copy real keys into this file or into any file under version control.",
    "Preferred credential source is the environment:",
    "  export ALPACA_API_KEY=...",
    "  export ALPACA_API_SECRET=...",
    "If you must use a local file for development, copy this file to",
    "'alpaca_credentials.json' (gitignored) and fill it in. Never commit it.",
    "Any credential that has ever been committed to git must be REVOKED at the",
    "provider and replaced - deleting the file does not undo the exposure."
  ],
  "alpaca_paper": {
    "api_key": "REPLACE_ME_NOT_A_REAL_ALPACA_KEY",
    "api_secret": "REPLACE_ME_NOT_A_REAL_ALPACA_SECRET",
    "exchange": "alpaca",
    "environment": "paper"
  }
}
